{% macro configWarning(setting) -%}
    {{ 'This is being overridden by the `{setting}` config setting.'|t('sherlock', {setting: setting })|markdown(inlineOnly=true) }}
{%- endmacro %}


{% macro info(text, params) -%}
    <span class="info">
        {{- text|t('sherlock', params)|markdown(inlineOnly=true) -}}
    </span>
{%- endmacro %}
